name: 🤖 GitFlow Pro - Simple CI/CD

on:
  push:
    branches: [ ci-test ]
  workflow_dispatch:

jobs:
  test:
    name: 🔍 Test Phase
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: List files
        run: |
          echo "✅ CI/CD is working!"
          ls -la
          
      - name: Create report
        run: |
          echo "# Test Report" > report.md
          echo "Status: PASS" >> report.md
          
      - name: Upload report
        uses: actions/upload-artifact@v4
        with:
          name: test-report
          path: report.md

  deploy:
    name: 🚀 Deploy Phase
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push'
    
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Deploy simulation
        run: |
          echo "🚀 This is where deployment would happen"
          echo "GitFlow Pro would auto-deploy here"
          
      - name: Health check
        run: |
          echo "✅ Deployment complete"